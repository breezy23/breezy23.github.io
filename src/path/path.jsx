import * as React from 'react';
import './path.scss';

class Path extends React.Component {
    render() {
        return (
            <div
                className={'path'}
            >
                *nuzzles pounces on u* uwu so warm
            </div>
        );
    };
}

export default Path;
